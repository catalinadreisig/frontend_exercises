/*const nextEpisodeButton = document.getElementById("nextEpisodeButton");

nextEpisodeButton.addEventListener("mouseenter", function () {
  nextEpisodeButton.classList.add("hovered");
});

nextEpisodeButton.addEventListener("mouseleave", function () {
  nextEpisodeButton.classList.remove("hovered");
});
*/
